spring:
  datasource:
    url: jdbc:mysql://db_incidencias:3306/db_incidencias
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false